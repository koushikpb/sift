# Phase 0 — Foundation: Eval Report

**Date:** 2026-06-24
**Gate:** corpus loaded · parser preserves hierarchy on 10 contracts · eval set v1 (50 items). **Result: PASS.**

## Corpus loaded (pgvector)
- Documents: 172  (49 CUAD, 123 ContractNLI)
- Clauses (hierarchy nodes): 5792
- Source command: `make ingest && make parse && make load`

## Structure-aware parser
- 10 sample contracts in `pipeline/fixtures/contracts/`.
- Invariants enforced by `test_parse_gate.py`: exact offsets (`text == raw[start:end]`),
  no dangling parents, root depth 0, post-parse ParsedDocument validates.
- Node-count snapshot: `pipeline/fixtures/expected_hierarchy.json`.
- Hand-verified by eye: contractnli_444, contractnli_465, contractnli_65 (trees printed
  in task-10 report); additionally contractnli_213, contractnli_30, contractnli_36,
  contractnli_395, contractnli_565, contractnli_59, contractnli_68.

## Eval set v1
- File: `evals/data/eval-set-v1.json` — 50 items.
- Categories: clean 22, deviated 15, missing 13; refusal items: 3.
- Graders: span_match 22, flag_match 25, refusal 3.
- Every gold span verified to resolve against the loaded corpus (`make eval-validate`).
- Playbook: `evals/playbook/nda.yaml` (7 positions).

## Notes / deviations
- CUAD sourced from `CUAD_v1.json` (SQuAD format) because the `cuad-qa` HF dataset is
  script-based (`datasets>=3` rejects it with "Dataset scripts are no longer supported").
- The 10 parser fixtures are all ContractNLI NDAs; CUAD download via HF was fixed mid-build
  by switching to direct JSON load, but fixture selection had already been made.
- Parser dedup-suffix fix (`--{k}`) applied to avoid `node_id` PK collisions when the same
  section label appears multiple times in a single contract.
- Refusal items (3) modeled as `category: missing` + `grader: refusal` to represent the
  "refuse when ungrounded" requirement from PLAN.md.

## Next: Phase 1 (Naive baseline)
Single-embedding top-k RAG, clause cards with source-span citations, SSE UI shell;
record baseline recall@k / NDCG@10 + groundedness.
